# ── LuzIA Frontend – Variáveis de Ambiente ──────────────────
#
# Para desenvolvimento local (backend rodando na máquina):
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
#
# Para produção (backend no Render):
NEXT_PUBLIC_API_BASE_URL=https://luzia-backend.onrender.com/api/v1

# Usa proxy interno /api/proxy por padrão para evitar problemas de CORS no browser.
# Defina "false" apenas se quiser chamadas diretas no cliente.
NEXT_PUBLIC_USE_API_PROXY=true

# Opcional (somente server-side): sobrescreve URL do backend usada pelo proxy.
# BACKEND_API_BASE_URL=https://luzia-backend.onrender.com/api/v1
